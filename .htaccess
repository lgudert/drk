
# BEGIN WordPress
# Die Anweisungen (Zeilen) zwischen „BEGIN WordPress“ und „END WordPress“ sind
# dynamisch generiert und sollten nur über WordPress-Filter geändert werden.
# Alle Änderungen an den Anweisungen zwischen diesen Markierungen werden überschrieben.
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]
RewriteBase /drkselm2/
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /drkselm2/index.php [L]
</IfModule>

# END WordPress
# BEGIN Simply Static
# Die Anweisungen (Zeilen) zwischen „BEGIN Simply Static“ und „END Simply Static“ sind
# dynamisch generiert und sollten nur über WordPress-Filter geändert werden.
# Alle Änderungen an den Anweisungen zwischen diesen Markierungen werden überschrieben.
<Files "C:\xampp\htdocs\drk/wp-content/uploads\simply-static\\dffa542ed11cec7947591cf26a4825ac-debug.txt">
require all denied
require host localhost
</Files>
# END Simply Static